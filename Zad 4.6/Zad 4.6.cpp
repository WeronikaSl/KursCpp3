#include <iostream>
#include <string>
#include <algorithm>

std::string toLower(std::string str)
{
    std::string strLow = for_each(str.begin(), data.end(), [](char& c) {c = ::tolower(c);
}

int main()
{
    std::cout << "Hello World!\n";
}
